name: hello

on:
  push:
   branches: [main]
jobs:
 greet:
   runs-on: fidora-latest
   steps:
   - name: say hello to users
     run: echo"hello dosto"
   - name: say hello to username
     run: echo"hello"
 
